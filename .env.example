VITE_SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Server-side only — never prefix with VITE_ (that would bundle it into client JS),
# never commit a real value, and never add it to the Cloudflare Pages build env.
# Only needed locally to run `npm run create-admin`.
SUPABASE_SERVICE_ROLE_KEY=
